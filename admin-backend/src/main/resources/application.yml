server:
  servlet:
    context-path: /api
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my_admin_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: boot
    password: 123456
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml